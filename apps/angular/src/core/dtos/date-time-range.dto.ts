/** Date-time range dto. */
export type DateTimeRangeDto = {

	/**
	 * Start.
	 * @type {string} - String in ISO date format.
	 * */
	readonly start: string;

	/**
	 * End.
	 * @type {string} - String in ISO date format.
	 * */
	readonly end: string;
};
