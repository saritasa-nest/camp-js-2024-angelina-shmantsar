/** Date-time range dto. */
export type DateTimeRangeDto = {

	/**
	 * Start.
	 * String in ISO date format.
	 * */
	readonly start: string;

	/**
	 * End.
	 * String in ISO date format.
	 * */
	readonly end: string;
};
